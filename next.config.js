/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['picsum.photos', 'media4.giphy.com', 'ibb.co', 'i.ibb.co'],
  },
}

module.exports = nextConfig
